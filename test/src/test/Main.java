package test;

public class Main {

    public static void main(String[] args) {
    	int count=15;
	int [] arr=new int[15];
	for (int i=0; i< arr.length;i++){
	    arr[i]= count;

	    System.out.print(arr[i] + " ");
		count++;
    }
	int max=arr[0];
	int min=arr[0];
	int avg=
	for(int i=0;i< arr.length;i++) {
		if (max<arr[i])
			max=arr[i];
		if (min>arr[i])
			min=arr[i];
	}

    }
}
